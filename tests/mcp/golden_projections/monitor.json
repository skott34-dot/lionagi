{
  "path": "monitor",
  "schema": {
    "additionalProperties": false,
    "description": "Show all running entities (sessions, invocations, shows, plays) in a compact table, or drill into one entity by ID. Use `watch` for live refresh.",
    "properties": {
      "chain": {
        "default": true,
        "description": "With `run_ids`: watch only the literal id(s) given, instead of following scheduler on_success/on_fail chain children by default.",
        "type": "boolean",
        "x-flag": "--no-chain"
      },
      "entity_type": {
        "description": "Narrow the table to one kind of entity: session, invocation, show or play.",
        "enum": [
          "session",
          "invocation",
          "show",
          "play"
        ],
        "type": "string",
        "x-aliases": [
          "-t"
        ],
        "x-flag": "--type"
      },
      "follow": {
        "default": false,
        "description": "With `run_ids`: keep discovering schedule_runs created after the initial set drains, instead of exiting once it is empty. Never ends on its own \u2014 pair with `max_wait`.",
        "type": "boolean",
        "x-flag": "--follow"
      },
      "id": {
        "description": "Run, session, play or show to open in the detail view \u2014 full id, or an unambiguous prefix. Omit it for the table of everything in flight.",
        "type": "string",
        "x-positional": true
      },
      "interval": {
        "default": 3.0,
        "description": "Seconds between state.db polls in `run_ids` mode (default 3). Independent of `refresh`, which paces the table redraw.",
        "type": "number",
        "x-flag": "--interval"
      },
      "max_wait": {
        "description": "With `run_ids`: give up waiting after this many seconds and exit EXIT_RUNNING for whatever is still pending, instead of blocking forever on a stuck session or run. Default: 900s. Pass 0 for unlimited.",
        "type": "number",
        "x-flag": "--max-wait"
      },
      "project": {
        "description": "Filter sessions and plays to one project name, matched exactly \u2014 the same name the table's project column shows.",
        "type": "string",
        "x-aliases": [
          "-p"
        ],
        "x-flag": "--project"
      },
      "refresh": {
        "default": 2,
        "description": "Seconds between redraws under `watch` (default 2). Ignored without it.",
        "type": "integer",
        "x-flag": "--refresh"
      },
      "run_ids": {
        "description": "Wait for one or more schedule_run IDs to reach a terminal state, then exit (scripting primitive; see `li monitor run --help` for the positional form). Follows scheduler on_success/on_fail chain children by default (see `chain`). Ignores id/`watch` / `since` / `entity_type` / `project`.",
        "type": "string",
        "x-flag": "--run"
      },
      "since": {
        "description": "Only show entities updated within this time window. Format: 30m, 1h, 2d (minutes/hours/days). Default: all running.",
        "type": "string",
        "x-flag": "--since"
      },
      "watch": {
        "default": false,
        "description": "Redraw the view every `refresh` seconds until interrupted, instead of printing one snapshot and exiting.",
        "type": "boolean",
        "x-aliases": [
          "-w"
        ],
        "x-flag": "--watch"
      }
    },
    "title": "monitor",
    "type": "object",
    "x-positional-order": [
      "id"
    ]
  },
  "stage": "static"
}
